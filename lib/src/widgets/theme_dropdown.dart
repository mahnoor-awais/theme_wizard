/// A dropdown widget for switching themes.
///
/// Placeholder for future implementation. This widget will expose an API for
/// selecting among app-provided themes and notifying listeners. No logic is
/// implemented at this stage.
import 'package:flutter/material.dart';

class ThemeDropdown extends StatelessWidget {
  ThemeDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox.shrink();
  }
}
